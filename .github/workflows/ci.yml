name: CI

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Build and test
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up pnpm and Node.js
        uses: pnpm/setup@v1
        with:
          version: 11.23.0
          runtime: node@24
          cache: true

      - name: Set up Python
        id: setup-python
        uses: actions/setup-python@v7
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: CI=true pnpm install --frozen-lockfile

      - name: Build
        run: pnpm build

      - name: Run TypeScript tests
        env:
          NOBEI_PHASE1_PYTHON: ${{ steps.setup-python.outputs.python-path }}
        run: pnpm exec vitest run --testTimeout 15000

      - name: Run Python tests
        env:
          NOBEI_PHASE1_PYTHON: ${{ steps.setup-python.outputs.python-path }}
        run: pnpm test:phase1b-python
